export interface ReservationDuration {
    name: string;
    minutes: number;
    isActive: boolean;
}

export interface ReservationDurationDTO {
    name: string;
    minutes: number;
    isActive: boolean;
}