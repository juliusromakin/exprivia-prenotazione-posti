export interface ReservationDuration {
    id?: number;
    name: string;
    minutes: number;
    isActive: boolean;
}

export interface ReservationDurationDTO {
    id?: number;
    name: string;
    minutes: number;
    isActive: boolean;
}